"""Sphinx configuration for {{ project_name }} documentation.

This file contains the configuration settings for building the Sphinx documentation
for the {{ project_name }} project.

This file only contains a selection of the most common options. For a full
list see the documentation:
https://www.sphinx-doc.org/en/master/usage/configuration.html
"""

# -- Path setup --------------------------------------------------------------

# If extensions (or modules to document with autodoc) are in another directory,
# add these directories to sys.path here. If the directory is relative to the
# documentation root, use os.path.abspath to make it absolute, like shown here.
#
import os
import sys

sys.path.insert(0, os.path.abspath("../.."))

# -- Project information -----------------------------------------------------

project = "{{ project_name }}"
copyright = "{{ copyright_year }}, {{ author_name }}"
author = "{{ author_name }}"

# Dynamic version detection
try:
    import importlib.metadata
    release = importlib.metadata.version("{{ project_slug }}")
except importlib.metadata.PackageNotFoundError:
    release = "{{ version }}"

version = ".".join(release.split(".")[:2])

# -- General configuration ---------------------------------------------------

# Add any Sphinx extension module names here, as strings. They can be
# extensions coming with Sphinx (named 'sphinx.ext.*') or your custom
# ones.
extensions = [
    "sphinx.ext.autodoc",
    "sphinx.ext.napoleon",
    "sphinx.ext.viewcode",
    "sphinx.ext.githubpages",
    "sphinx.ext.intersphinx",
    "sphinx.ext.todo",
    "sphinx.ext.coverage",
    "sphinx.ext.mathjax",
    "sphinx.ext.ifconfig",
    "sphinx.ext.autosummary",
    "sphinx.ext.doctest",
    "sphinx_copybutton",
    "sphinx_autodoc_typehints",
    "myst_parser",  # Add MyST parser for Markdown support
]

# Add any paths that contain templates here, relative to this directory.
# templates_path = ["_templates"]

# List of patterns, relative to source directory, that match files and
# directories to ignore when looking for source files.
# This pattern also affects html_static_path and html_extra_path.
exclude_patterns: list[str] = [
    "_templates/custom-*.rst",  # Exclude autodoc template files
    "_templates/README.md",     # Exclude template documentation
]

# Source file suffixes
source_suffix = {
    ".rst": "restructuredtext",
    ".md": "markdown",
}

# Master document for consistent navigation
master_doc = "master"

# -- Options for HTML output -------------------------------------------------

# The theme to use for HTML and HTML Help pages.  See the documentation for
# a list of builtin themes.
html_theme = "furo"  # Changed from sphinx_rtd_theme to furo

# Furo theme options
html_theme_options = {
    "light_css_variables": {
        "color-brand-primary": "#7C3AED",
        "color-brand-content": "#7C3AED",
        "color-accent": "#7C3AED",
        "color-background-primary": "#ffffff",
        "color-background-secondary": "#f8fafc",
        "color-background-border": "#e5e7eb",
        "color-foreground-primary": "#111827",
        "color-foreground-secondary": "#6b7280",
        "color-foreground-muted": "#9ca3af",
        "color-foreground-border": "#d1d5db",
        "font-stack": "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif",
        "font-stack--monospace": "ui-monospace, SFMono-Regular, 'SF Mono', Consolas, 'Liberation Mono', Menlo, monospace",
    },
    "dark_css_variables": {
        "color-brand-primary": "#A78BFA",
        "color-brand-content": "#A78BFA",
        "color-accent": "#A78BFA",
        "color-background-primary": "#0f172a",
        "color-background-secondary": "#1e293b",
        "color-background-border": "#334155",
        "color-foreground-primary": "#f8fafc",
        "color-foreground-secondary": "#cbd5e1",
        "color-foreground-muted": "#94a3b8",
        "color-foreground-border": "#475569",
    },
    "navigation_with_keys": True,
    "announcement": "<em>🌱 Generated with <a href='https://github.com/jeffrichley/seedling-template'>Seedling</a></em>",
    "sidebar_hide_name": False,
    "top_of_page_button": "edit",
    "source_repository": "https://github.com/{{ github_username }}/{{ project_slug }}",
    "source_branch": "main",
    "source_directory": "docs/source/",
}

# Add any paths that contain custom static files (such as style sheets) here,
# relative to this directory. They are copied after the builtin static files,
# so a file named "default.css" will overwrite the builtin "default.css".
html_static_path = ["_static"]

# Custom CSS file
html_css_files = ["custom.css"]

# -- Extension configuration -------------------------------------------------

# Autodoc settings
autodoc_default_options = {
    "members": True,
    "member-order": "bysource",
    "special-members": "__init__",
    "undoc-members": True,
    "exclude-members": "__weakref__",
    "show-inheritance": True,
    "inherited-members": True,
    "no-value": True,
}

# Make autodoc prettier - following Furo example
autodoc_preserve_defaults = True
autodoc_class_signature = "separated"
autodoc_member_order = "bysource"
autodoc_default_flags = ["members", "undoc-members", "show-inheritance"]

# MyST settings
myst_enable_extensions = [
    "colon_fence",
    "deflist",
    "dollarmath",
    "fieldlist",
    "html_admonition",
    "html_image",
    "replacements",
    "smartquotes",
    "strikethrough",
    "substitution",
    "tasklist",
]

# Intersphinx mapping for external references
intersphinx_mapping = {
    "python": ("https://docs.python.org/3/", None),
    "sphinx": ("https://www.sphinx-doc.org/en/master/", None),
}

# Todo settings
todo_include_todos = True
todo_link_only = True

# Copy button settings
copybutton_prompt_text = r">>> |\.\.\. |\$ |In \[\d*\]: | {2,5}\.\.\.: | {5,8}: "
copybutton_prompt_is_regexp = True
copybutton_line_continuation_character = "\\"

# Autodoc type hints settings
autodoc_typehints = "description"
autodoc_typehints_format = "short"
autodoc_typehints_description_target = "documented"

# Autosummary settings for prettier API docs
autosummary_generate = True
autosummary_imported_members = True
autosummary_ignore_module_all = False

# Enable strict cross-reference checking
nitpicky = False

# Suppress warnings for generic type variables and standard library references
nitpick_ignore = [
    ("py:class", "enum.Enum"),
    ("py:class", "abc.ABC"),
    ("py:class", "collections.abc.Callable"),
    ("py:class", "collections.abc.Awaitable"),
]

# Suppress specific warning types
suppress_warnings = [
    "ref.python",  # Suppress cross-reference warnings
]


