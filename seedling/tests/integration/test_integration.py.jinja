"""Integration tests for {{ project_name }}."""

import pytest

from {{ project_slug }} import config, main_function


@pytest.mark.integration
def test_main_function_with_configuration() -> None:
    """Test that main_function works with configuration changes."""
    # Arrange
    input_data = "test"
    original_prefix = config.get_setting("processing_prefix")
    config.set_setting("processing_prefix", "Result: ")
    
    # Act
    result = main_function(input_data)
    
    # Assert
    assert result == "Result: test"
    
    # Cleanup - restore original configuration
    config.set_setting("processing_prefix", original_prefix) 