"""Functional tests for {{ project_name }}."""

import pytest

from {{ project_slug }} import main_function


@pytest.mark.functional
def test_main_function_with_business_rules() -> None:
    """Test that main_function enforces business rules correctly."""
    # Arrange
    input_data = "valid input"
    
    # Act
    result = main_function(input_data)
    
    # Assert
    assert isinstance(result, str)
    assert result.startswith("Processed: ")
    assert result == "Processed: valid input" 